<Window x:Class="GrammarAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Grammar Assistant" Height="450" Width="750"
        MinHeight="360" MinWidth="600">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="18" FontWeight="SemiBold" Text="Windows Grammar Assistant" />

        <TextBox x:Name="InputText" Grid.Row="1" Margin="0,8,0,8" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontSize="14" ToolTip="Type or paste text here..." />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button x:Name="CheckTextButton" Content="Check Text" Width="100" Click="OnCheckTextClick" />
            <Button x:Name="CheckClipboardButton" Content="Check Clipboard" Width="130" Click="OnCheckClipboardClick" />
            <TextBlock VerticalAlignment="Center" Text="Use Ctrl+Shift+G to pull the active selection into the clipboard and paste it here." TextWrapping="Wrap" />
        </StackPanel>

        <Border Grid.Row="3" BorderBrush="LightGray" BorderThickness="1" CornerRadius="4">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock FontSize="16" FontWeight="SemiBold" Text="Suggestions" />
                <ListBox x:Name="SuggestionsList" Grid.Row="1" Margin="0,6,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding SuggestionText}" FontStyle="Italic" Foreground="Gray" TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>
